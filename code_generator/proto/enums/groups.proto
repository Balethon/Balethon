syntax = "proto3";

package enums;

enum Restriction {
  RESTRICTION_PRIVATE = 0;
  RESTRICTION_PUBLIC = 1;
}

enum GroupType {
  GROUP_TYPE_GROUP = 0;
  GROUP_TYPE_CHANNEL = 1;
  GROUP_TYPE_SUPER_GROUP = 2;
}