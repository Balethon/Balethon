## class balethon.objects.*Invoice*

**این کلاس به عنوان یک صورتحساب ایفای نقش میکند**

### پارامترها

- **title** (`str`)
    > تیتر صورتحساب

- **description** (`str`)
    > توضیحات صورتحساب
    
- **start_parameter** (`str`)
    > پارامتر ابتدایی صورتحساب

- **currency** (`str`)
    > واحد پول صورتحساب
    
- **total_amount** (`int`)
    > مبلغ کل