## class balethon.objects.*Photo*

**این کلاس به عنوان یک عکس ایفای نقش میکند**

### پارامترها

- **id** (`str`)
    > آیدی عکس

- **width** (`int`)
    > عرض عکس

- **height** (`int`)
    > طول عکس
    
- **size** (`int`)
    > سایز عکس