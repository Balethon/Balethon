## class balethon.objects.*Voice*

**این کلاس به عنوان یک صدا ایفای نقش میکند**

### پارامترها

- **id** (`str`)
    > آیدی صدا

- **duration** (`int`)
    > مدت زمان صدا

- **mime_type** (`str`)
    > نوع فرمت صدا
    
- **size** (`int`)
    > سایز صدا