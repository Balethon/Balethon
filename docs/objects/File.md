## class balethon.objects.*File*

**این کلاس به عنوان یک فایل ایفای نقش میکند**

### پارامترها

- **id** (`str`)
    > آیدی فایل

- **size** (`int`)
    > سایز فایل

- **path** (`str`)
    >  مسیر فایل
