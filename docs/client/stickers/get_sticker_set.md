## Client.*get_sticker_set()*

**دریافت اطلاعات یک ست استیکر**

### پارامترها

- **name** (`str`)
    > اسم ست استیکری که اطلاعات آن دریافت میشود

### مقدار بازگشتی

> `StickerSet`

### مثال

```python
await bot.get_sticker_set("پیشی")
```