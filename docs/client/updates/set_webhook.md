## Client.*set_webhook()*

**تنظیم وب هوک و دریافت آپدیت‌ ها از طریق http**

### پارامترها

- **url** (`str`)
    > آدرسی که آپدیت ها به آن ارسال میشوند (اختیاری)

### مقدار بازگشتی

> `bool`

### مثال

```python
await bot.set_webhook("URL")
```