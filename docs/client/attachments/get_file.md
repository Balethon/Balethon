## Client.*get_file()*

**دریافت اطلاعات یک فایل**

### پارامترها

- **file_id** (`str` | `int`)
    > آیدی فایلی که اطلاعات آن دریافت میشود

### مقدار بازگشتی

> `File`

### مثال

```python
await bot.get_file("FILE_ID")
```